---
phase: 03-advanced-features
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - server/src/routes/
  - server/src/controllers/
  - client/app/
  - client/components/
  - client/utils/api.ts
autonomous: true
requirements:
  - ADV-04
  - ADV-05
must_haves:
  truths:
    - "User receives and can act on supported notifications."
    - "User can follow users and post/read market comments."
  artifacts:
    - path: "server/src/routes/"
      provides: "notification + social endpoint contracts"
    - path: "client/components/"
      provides: "comment/follow interaction UI"
  key_links:
    - from: "notification payload"
      to: "client route"
      via: "deep-link target"
      pattern: "market|leaderboard|profile"
---

<objective>
Ship notifications and social baseline (follow/comments) with secure contracts and client UI integration.
</objective>

<context>
@.planning/phases/03-advanced-features/03-RESEARCH.md
@docs/SECURITY_AUDIT.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add notification/social backend contracts and tests</name>
  <files>server/tests/contracts/notifications-social.contract.test.ts, server/src/routes/, server/src/controllers/</files>
  <behavior>
    - Notification trigger endpoints validate payload and auth.
    - Follow/comment endpoints enforce auth and return expected shapes.
  </behavior>
  <action>Write contracts first, then implement route/controller handlers for notification registration/triggers and follow/comment primitives.</action>
  <verify>
    <automated>cd server; npm run test -- tests/contracts/notifications-social.contract.test.ts</automated>
  </verify>
  <done>Backend social/notification contracts are implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Add client notification handling and social UI wiring</name>
  <files>client/app/, client/components/, client/utils/api.ts</files>
  <action>Wire API calls for follow/comment and notification registration/handling, including deep-link navigation targets for supported notification types.</action>
  <verify>
    <automated>cd client; npx tsc --noEmit</automated>
  </verify>
  <done>Client can consume notification payloads and interact with social baseline features.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->social API | user-generated text and follow actions |
| server->push channel | notification payload sent to external channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | comment payload | mitigate | validate length/content and sanitize persisted text |
| T-03-08 | S | follow/comment auth | mitigate | require auth and use server-side user id only |
| T-03-09 | I | notification payload | mitigate | include only minimal fields and signed deep-link target |
</threat_model>

<verification>
Run notifications/social contract tests and client typecheck.
</verification>

<success_criteria>
Notifications and social baseline work with secure validated contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-features/03-04-SUMMARY.md`
</output>
