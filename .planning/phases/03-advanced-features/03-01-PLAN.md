---
phase: 03-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/routes/marketRoutes.ts
  - server/src/controllers/marketController.ts
  - client/app/marketDetails.tsx
  - client/components/market/
autonomous: true
requirements:
  - ADV-01
must_haves:
  truths:
    - "User can submit market creation form with required fields."
    - "User-created market is pending until admin approval."
    - "Only admin-approved markets become publicly visible/tradable."
    - "Invalid market creation input returns clear 4xx errors."
  artifacts:
    - path: "server/src/controllers/marketController.ts"
      provides: "Create-market validation, pending state, and admin approval transition handler"
    - path: "client/app/marketDetails.tsx"
      provides: "Market creation UI flow"
  key_links:
    - from: "client/app/marketDetails.tsx"
      to: "server market create endpoint"
      via: "api.post"
      pattern: "create|market"
---

<objective>
Ship market creation baseline with admin approval gate (UI + API validation + persistence + review workflow).
</objective>

<context>
@.planning/phases/03-advanced-features/03-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add create-market API contract tests and validation</name>
  <files>server/tests/contracts/market-create.contract.test.ts, server/src/controllers/marketController.ts, server/src/routes/marketRoutes.ts</files>
  <behavior>
    - Valid user payload returns success with pending-review market state.
    - Missing title/description/category/resolution date returns 400.
    - Pending markets are not returned by public list/detail endpoints.
    - Admin approval endpoint transitions pending market to public visibility state.
  </behavior>
  <action>Create contract test first, then implement route/controller validation with role-gated admin approval transition to satisfy test behavior.</action>
  <verify>
    <automated>cd server; npm run test -- tests/contracts/market-create.contract.test.ts</automated>
  </verify>
  <done>Market creation endpoint has deterministic pending-review behavior, approval transition, and contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire client market creation flow to backend contract</name>
  <files>client/app/marketDetails.tsx, client/utils/api.ts</files>
  <action>Implement submit path from client form to create-market endpoint with loading/error handling and explicit pending-approval user feedback consistent with current app patterns.</action>
  <verify>
    <automated>cd client; npx tsc --noEmit</automated>
  </verify>
  <done>User can create market from client flow and sees pending-approval status messaging until admin review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->create-market API | Untrusted form input enters backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | create-market payload | mitigate | validate required fields and allowed values before persistence |
| T-03-02 | E | create-market route | mitigate | enforce auth for market mutation endpoint and admin-only approval transition |
</threat_model>

<verification>
Run create-market contract test and client typecheck.
</verification>

<success_criteria>
Market creation works from client and backend with validated contract behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-features/03-01-SUMMARY.md`
</output>
