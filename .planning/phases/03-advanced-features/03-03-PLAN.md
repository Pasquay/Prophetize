---
phase: 03-advanced-features
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - server/src/index.ts
  - server/src/services/
  - client/context/
  - client/app/tabs/home.tsx
  - client/app/tabs/leaderboard.tsx
autonomous: true
requirements:
  - ADV-03
must_haves:
  truths:
    - "User sees live leaderboard/market updates without manual refresh."
    - "Client reconnect recovers latest state after disconnect."
  artifacts:
    - path: "server/src/index.ts"
      provides: "Socket transport wiring and event emission"
    - path: "client/context/"
      provides: "Socket subscription lifecycle"
  key_links:
    - from: "server socket events"
      to: "client subscribed screens"
      via: "socket.io"
      pattern: "market.updated|leaderboard.updated|portfolio.updated"
---

<objective>
Deliver realtime transport for market/leaderboard/portfolio update events.
</objective>

<context>
@.planning/phases/03-advanced-features/03-RESEARCH.md
@.planning/codebase/INTEGRATIONS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add realtime contract tests and server event emitters</name>
  <files>server/tests/contracts/realtime.contract.test.ts, server/src/index.ts</files>
  <behavior>
    - Trade and market updates emit expected event payload names.
    - Missing subscription does not crash server.
  </behavior>
  <action>Create socket contract tests and implement event emission points in backend update paths.</action>
  <verify>
    <automated>cd server; npm run test -- tests/contracts/realtime.contract.test.ts</automated>
  </verify>
  <done>Server emits stable realtime event contracts for subscribed clients.</done>
</task>

<task type="auto">
  <name>Task 2: Add client subscription + reconnect resync behavior</name>
  <files>client/context/, client/app/tabs/home.tsx, client/app/tabs/leaderboard.tsx</files>
  <action>Implement socket connect/disconnect lifecycle, bind events to state update handlers, and trigger resync fetch on reconnect.</action>
  <verify>
    <automated>cd client; npx tsc --noEmit</automated>
  </verify>
  <done>Live updates apply in subscribed screens and recover on reconnect.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket server->client | Event payloads cross process/network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | I | event payload shape | mitigate | emit only whitelist fields and avoid sensitive profile data |
| T-03-06 | D | reconnect storm | mitigate | throttle reconnect and resync path |
</threat_model>

<verification>
Run realtime contract test and client typecheck.
</verification>

<success_criteria>
Realtime updates work across tabs and reconnect path restores fresh state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-features/03-03-SUMMARY.md`
</output>
