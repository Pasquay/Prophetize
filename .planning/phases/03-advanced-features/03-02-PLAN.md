---
phase: 03-advanced-features
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - server/src/controllers/transactionController.ts
  - server/src/routes/transactionRoutes.ts
  - client/app/marketDetails.tsx
  - client/context/useUserStore.tsx
autonomous: true
requirements:
  - ADV-02
must_haves:
  truths:
    - "User can buy/sell shares with clear success and error feedback."
    - "Balance and position refresh after trade commit."
  artifacts:
    - path: "server/src/controllers/transactionController.ts"
      provides: "Trade request validation and execution"
    - path: "client/app/marketDetails.tsx"
      provides: "Trade UI actions"
  key_links:
    - from: "client/app/marketDetails.tsx"
      to: "transaction routes"
      via: "api.post"
      pattern: "buy|sell"
---

<objective>
Ship hardened trading interface with validated buy/sell flow and state refresh.
</objective>

<context>
@.planning/phases/03-advanced-features/03-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add trading contract tests and enforce backend validation</name>
  <files>server/tests/contracts/trading.contract.test.ts, server/src/controllers/transactionController.ts</files>
  <behavior>
    - Invalid shares/price/market status return 4xx.
    - Valid buy/sell returns updated balance snapshot.
  </behavior>
  <action>Write tests first, implement validation and deterministic response contracts in controller/service boundary.</action>
  <verify>
    <automated>cd server; npm run test -- tests/contracts/trading.contract.test.ts</automated>
  </verify>
  <done>Trading endpoint behavior is contract tested and stable.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate buy/sell UI with optimistic refresh guardrails</name>
  <files>client/app/marketDetails.tsx, client/context/useUserStore.tsx</files>
  <action>Wire trade actions to API, prevent duplicate submits, and refresh balance/position state after successful trade.</action>
  <verify>
    <automated>cd client; npx tsc --noEmit</automated>
  </verify>
  <done>User can trade without duplicate-submit race and sees updated account state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->trade API | Monetary-like action with untrusted payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T | trade payload | mitigate | validate numeric bounds and market status server-side |
| T-03-04 | D | repeated trade submit | mitigate | deduplicate client submits and reject replay patterns |
</threat_model>

<verification>
Run trading contract tests and client typecheck.
</verification>

<success_criteria>
Trading flow behaves correctly for valid and invalid cases, with post-trade state updates.
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-features/03-02-SUMMARY.md`
</output>
