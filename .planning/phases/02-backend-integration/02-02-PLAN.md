---
phase: 02-backend-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - server/tests/contracts/auth-refresh.contract.test.ts
  - server/docs/leaderboard-categories-rollback.md
autonomous: true
requirements:
  - BH-AUTH-01
must_haves:
  truths:
    - "Rollback procedure is documented and executable for leaderboard/categories deployment."
    - "Refresh-token contract remains compatible after backend integration changes."
  artifacts:
    - path: "server/tests/contracts/auth-refresh.contract.test.ts"
      provides: "Compatibility lock for POST /auth/refresh-token request/response"
    - path: "server/docs/leaderboard-categories-rollback.md"
      provides: "Rollback/runbook and revert validation commands"
  key_links:
    - from: "server/tests/contracts/auth-refresh.contract.test.ts"
      to: "server/src/controllers/userController.ts"
      via: "HTTP contract assertions for refresh-token payload and response"
      pattern: "refresh-token|access_token"
    - from: "server/docs/leaderboard-categories-rollback.md"
      to: "server/src/index.ts"
      via: "Route unmount/restore instructions during rollback"
      pattern: "app\.use\('/leaderboard'"
---

<objective>
Create rollback runbook for leaderboard/categories backend deployment.

Purpose: make revert path explicit and quickly executable if regressions occur.
Output: documented rollback with validation checks for auth refresh and endpoint continuity.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Write rollback runbook with smoke-check scripts</name>
  <files>server/tests/contracts/auth-refresh.contract.test.ts, server/docs/leaderboard-categories-rollback.md</files>
  <action>Add/strengthen auth-refresh compatibility contract test (`POST /auth/refresh-token` payload/response lock), then document rollback procedure covering: disable/unmount leaderboard routes, git-based revert steps, and pre/post rollback curl checks for `/markets/categories`, `/leaderboard`, `/leaderboard/me`, and auth-refresh continuity.</action>
  <verify>
    <automated>test -f server/docs/leaderboard-categories-rollback.md</automated>
  </verify>
  <done>Runbook exists with deterministic steps and expected status-code outcomes for both forward and rollback states.</done>
</task>

</tasks>

<success_criteria>
- Rollback runbook exists and is executable by backend team.
- Smoke checks include auth refresh compatibility and leaderboard/category route health.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration/02-02-SUMMARY.md`
</output>
